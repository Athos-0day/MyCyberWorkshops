# Défi 2

Ce défi va vous permettre de comprendre pourquoi la longueur de la clé est importante lorsqu'on souhaite chiffrer un message. 

On vous donne le fichier **cipher.bin** qui contient le flag fichier en format binaire donc pour le lire mode **rb** et un indice sur le flag ci-dessous.

## Indice sur le flag

Le format du flag est :
**Net{xxxxx}**


